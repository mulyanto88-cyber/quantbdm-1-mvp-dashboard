// Next.js uses app/ over src/app/ when both exist.
// All actual page content lives in src/app/ but routes must be defined here.
// We re-export everything from src/app/page.tsx

export { default } from '../src/app/page'
